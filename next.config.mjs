/** @type {import('next').NextConfig} */
const nextConfig = {
  // Static export — the site deploys as plain files (Vercel, GitHub Pages, any host).
  output: 'export',
};

export default nextConfig;
